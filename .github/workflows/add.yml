name: "add"

on:
  push:
    paths:
      - "services/add/**"
      - ".github/workflows/add.yaml"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: WORK WORK
        run: |-
          cd services/add
          npm ci
          npm test
